<section class="main__header">
    <div class="header__content">
        <h2>Editar marca</h2>
        <p>Complete toda la información a continuación</p>
    </div>

    <div class="header__btn-container">
        <button class="btn btn--gray" id="btnBack">
            <iconify-icon icon="tabler:xbox-x"></iconify-icon>
            <span>Cancelar</span>
        </button>
        <button class="btn btn--green" type="submit" form="form">
            <iconify-icon icon="tabler:circle-check"></iconify-icon>
            <span>Guardar</span>
        </button>
    </div>
</section>

<form id="form" class="main__body-column" method="POST" data-route="brand"  data-type="edit" data-id="<%= data.brand.brand_id %>">
    <section class="main__body-column__left">
        <section class="wrapper">
            <div class="wrapper__header wrapper__header--with-badge">
                <h2 class="wrapper__subtitle">General</h2>
                <label for="brand_status" class="label label--checkbox">
                    <input class="checkbox" type="checkbox" name="brand_status" id="brand_status" checked>
                    <span>Activo</span>
                </label>
            </div>

            <div class="wrapper__body">
                <div class="wrapper__row">
                    <label class="label">
                        <span>Nombre</span>
                        <input class="input-text" type="text" name="brand_name" value="<%= data.brand.brand_name %>" required>
                    </label>
                </div>
            </div>
        </section>

        <hr>

        <section class="wrapper wrapper--delete">
            <div class="wrapper__header">
                <h2 class="wrapper__subtitle">Eliminar</h2>
            </div>
            <div class="wrapper__body">
                <div class="wrapper__group">
                    <p>Elimina definitivamente la marca y todos los productos asociados. Esta acción no se puede deshacer</p>
                    <button id="btn-delete" class="btn btn--red"  type="button" data-id="<%= data.brand.brand_id %>" data-route="brand">Eliminar</button>
                </div>
            </div>
        </section>
    </section>

    <!-- ----------------------------------------------------------------------------  -->

    <section class="main__body-column__right">
        <section class="wrapper">
            <div class="wrapper__header">
                <h2 class="wrapper__subtitle">Indicadores</h2>
            </div>
            <div class="wrapper__body">
                <div class="wrapper__row wrapper__row--auto">
                    <label for="bf_new" class="label label--checkbox">
                        <input class="checkbox" type="checkbox" name="bf_new" id="bf_new" <%=data.brand.brand_flags.new == 1 ? 'checked' : '' %>>
                        <span>Nuevo</span>
                    </label>
                    <label for="bf_promoted" class="label label--checkbox">
                        <input class="checkbox" type="checkbox" name="bf_promoted" id="bf_promoted" <%=data.brand.brand_flags.promoted == 1 ? 'checked' : '' %>>
                        <span>Promocional</span>
                    </label>
                    <label for="bf_featured" class="label label--checkbox">
                        <input class="checkbox" type="checkbox" name="bf_featured" id="bf_featured" <%=data.brand.brand_flags.featured == 1 ? 'checked' : '' %>>
                        <span>Destacado</span>
                    </label>
                    <label for="bf_topseller" class="label label--checkbox">
                        <input class="checkbox" type="checkbox" name="bf_topseller" id="bf_topseller" <%=data.brand.brand_flags.topseller == 1 ? 'checked' : '' %>>
                        <span>Más vendido</span>
                    </label>
                </div>
            </div>
        </section>
        <section class="wrapper">
            <div class="wrapper__header">
                <h2 class="wrapper__subtitle">Imagenes</h2>
            </div>
            <div class="wrapper__body">
                <div class="wrapper__img-container">
                    <img src="" alt="">
                </div>
            </div>
            <div class="wrapper__footer">
                <input type="file" accept="image/*" name="brand_img" id="brand_img" value="<%= data.brand.brand_img %>" >
            </div>
        </section>
    </section>
</form>